<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sx.trackdispatch">

    <application
        android:name="com.sx.trackdispatch.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArmsLockerMVVM">

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="a88dbcf10087d23ef145e0b2785901a3"/>
        <service android:name="com.amap.api.location.APSService"/>

        <activity android:name="com.sx.trackdispatch.view.MainActivity" />
        <activity android:name="com.sx.trackdispatch.view.LoginActivity" />
        <activity android:name="com.sx.trackdispatch.view.SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.SettingActivity" />
        <activity android:name=".view.TransferOrderDetailActivity" />
    </application>

</manifest>